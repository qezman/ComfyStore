import Layout from "../Components/Layout";
import HeroContent from "../Components/HeroContent";

const Home = () => {
  return (
    <Layout>
      <HeroContent />
    </Layout>
  );
};

export default Home;
